# 组合包（Merge Package）打包规范

## 1. 概述

组合包是指将一个 `.amr` 前端包内同时携带多个设备型号的 C++ 动态库
（`libjsapi_bridge.so`），由前端打包器（`aiot-cli`）在打包阶段统一打入，
运行时由 UI 根据设备信息选择对应代号目录下的动态库加载。

> 注意：脚本中的 "SKU" 实为**设备代号（codename）**，并非库存单位。

入口脚本：`tools/build.sh --merge` → `build_merge()`。

## 2. 当前支持的设备代号

| 代号 | 型号 | 架构 / C 库 | libs 子目录 | 构建目录 |
|---|---|---|---|---|
| `orange` | X7 | aarch64 / glibc | `arm64-orange` | `jsapi/build_orange` |
| `y02-1` | X7p | aarch64 / glibc | `arm64-y02-1` | `jsapi/build_y02-1` |
| `coco` | X5 | armv7 / glibc | `arm-coco` | `jsapi/build_coco` |
| `plum` | S6 | armv7 / glibc | `arm-plum` | `jsapi/build_plum` |

## 3. 目录与产物约定

### 3.1 C++ 库产物

- 每个代号独立构建，产物统一命名为 `libjsapi_bridge.so`
- 构建目录：`jsapi/build_<代号>`
- 最终放置：`ui/libs/<架构>-<代号>/libjsapi_bridge.so`（strip 后）

### 3.2 前端打包产物

- 打包器将 `ui/libs/` 下所有子目录打进同一 `.amr`
- 最终产物：`dist/miniapp-merged.amr`

### 3.3 版本信息

- 版本号来源：`ui/package.json` 的 `version` 字段
- `--release` 时自动 `npm version patch --no-git-tag-version` 递增
- 打包时由 `tools/generate_ui_version.js` 生成
  `ui/src/res/version.js`（含 `clib`、`isCvi`、`nowVersion`、`buildCode`、
  `appid`、`updateDir` 等字段）

## 4. 打包流程

`build_merge()` 执行顺序：

1. 清空并重建 `ui/libs/`
2. 逐个调用 `build_lib_only()` 编译各代号动态库：

   - `resolve_profile` 解析工具链（arch/clib/ver）
   - `ensure_toolchain` 下载/解压或复用缓存工具链
   - `resolve_deps` 选择对应设备依赖目录（`DEPS_DEVICE`）
   - 导出环境变量：`CROSS_TOOLCHAIN_PREFIX`、`LOLIAPP_CLIB`、`ISCVI`
     - aarch64 → `ISCVI=false`
     - armv7 → `ISCVI=true`
   - `cmake -DTARGET_SKU=<代号> -DDEPS_ROOT=<deps>`
   - `make`，产物 `libjsapi_bridge.so` strip 后拷入 `ui/libs/<架构>-<代号>/`

3. 在 `ui/` 下执行 `npm run build:prod`
   （即 `gen:version` + `patch:toolchain` + `aiot-cli -c -q -p`）
4. 打包出的 `.amr` 移入 `dist/miniapp-merged.amr`

## 5. 新增设备代号步骤

1. 在 `build_merge()` 中追加一行，例如：

   ```bash
   build_lib_only "glibc32_x9" "armv7" "glibc" "" "x9" "$UI_LIBS/arm-x9"
   ```

2. 确认 `tools/config.sh` 中 `PROFILES` 已配置对应 arch/clib 的工具链
   （如新增架构/clib 组合），且 `jsapi/deps/<设备>` 依赖目录存在
3. 若前端需要针对新代号做差异化（如 `page_rotation_sku`），在
   `ui/package.json` 的 `props` 中补充
4. 执行 `tools/build.sh --merge` 验证

## 6. 注意事项

- `-DTARGET_SKU` 目前**未被 C++ 代码引用**，仅用于区分构建目录与
  `ui/libs/` 子目录；各代号产物差异实际仅由架构（ISCVI）与 C 库
  （LOLIAPP_CLIB）决定
- `ui/libs/` 中动态库统一命名 `libjsapi_bridge.so`，靠上层目录（代号）
  区分，勿改名
- 每次打包前会 `rm -f *.amr` 清理前端缓存，确保产物最新
- 单包模式（`build_target`）将 so 复制为 `ui/libs/libjsapi_bridge.so`，
  组合包模式下该平铺路径**不得存在**，否则与子目录方式冲突
- 工具链缓存在 `tmp/toolchains/<arch>-<clib>-<ver>`，已下载归档在
  `tmp/` 下，可复用
